# OS
.DS_Store

# Editors
.idea/
.vscode/

# Python
.venv/
venv/
**/__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# uv lockfile (CI installs with pip; drop this line if the team adopts uv)
uv.lock

# Build
dist/
build/
*.egg-info/

# CastleRAG generated data — never commit raw dataset or derived artifacts
data/derived/
data/manifests/
outputs/

# Embeddings cache
data/derived/embeddings/

# LoRA checkpoints
data/lora_checkpoints/

# Logs from SLURM jobs
logs/

# Environment config with secrets
.env

# Report / paper (private, not part of the submission)
/report.tex
/report.pdf
/report.aux
/report.log
/report.bbl
/report.blg
/report.out
/report.synctex.gz

# Course lecture slides (third-party material)
/slides/
.qdrant-initialized

# W&B local run artifacts
wandb/

# VPS demo deployment — local-only files, never commit
.vps_basic_auth_password
configs/vps_local.yaml

# Claude Code local state (a shared .claude/settings.json or skills stay committable)
.claude/settings.local.json
.claude/worktrees/
/memory/

# Playwright MCP captures and ad-hoc UI screenshots
.playwright-mcp/
/test-*.png
